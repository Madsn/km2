before_script:
  - echo "======================================"
  - echo "==== START auto full script v0.1 ====="
  - echo "======================================"

types:
  - build
  - test

job_build:
  type: build
  script:
  - meteor npm install
  only:
  - master
  tags:
  - master

job_test:
  type: test
  script:
  - echo "testing ----"
  - meteor npm test
  only:
  - master
  tags:
  - master